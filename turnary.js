const isObese = false;

console.log(isObese ? "Yesss" : "No00")

let cash = 50 

let price = 60

let isStoreOpen = true

let ans = cash >= price && isStoreOpen ? "give receipt" : "do not give receipt"

console.log(ans);